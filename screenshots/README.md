# Package screenshots

Media shown on [pub.dev](https://pub.dev/packages/manatal) and in the root `README.md`.

The example app also keeps copies under `example/screenshots/` (not included in the pub publish tarball).
